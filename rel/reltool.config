{sys, [
       {lib_dirs, ["../deps", "../../"]},
       {rel, "busket", "1",
        [
         kernel,
         stdlib,
         sasl,
         crypto,
         emongo,
         % mochiweb,
         % webmachine,
         busket
        ]},
       {rel, "start_clean", "",
        [
         kernel,
         stdlib
        ]},
       {boot_rel, "busket"},
       {profile, embedded},
       {excl_sys_filters, ["^bin/.*",
                           "^erts.*/bin/(dialyzer|typer)"]},
       {app, busket, [{incl_cond, include}]},
       {app, sasl, [{incl_cond, include}]}
      ]}.

{rebar, [
         {empty_dirs, [
                       "log/sasl"
                      ]},

         {overlay, "overlay"}
         ]}.
